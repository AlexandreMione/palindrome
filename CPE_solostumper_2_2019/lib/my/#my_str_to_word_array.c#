/*
** EPITECH PROJECT, 2020
** my_str_to_word_array
** File description:
** my_str_to_word_array
*/

#include "my.h"

int chara(char c)
{
    if (c >= '0' && c <= '9' || c >= 'A' && c <= 'Z' || c >= 'a' && x <= 'z')
        return (1);
}